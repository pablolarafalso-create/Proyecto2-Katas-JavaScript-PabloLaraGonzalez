const showMeButton = document.querySelector('.showme');
console.log(showMeButton);

const titulo = document.querySelector('#pillado');
console.log(titulo);

const paragraphs = document.querySelectorAll('p');
console.log(paragraphs);

const pokemon = document.querySelectorAll('.pokemon');
console.log(pokemon);

const dataFunction = document.querySelectorAll('[data-function]');
console.log(dataFunction);

const thirdCharacter = document.querySelectorAll('[data-function="testMe"]')[2];
console.log('1.6 Tercer personaje (índice 2):', thirdCharacter);
